- Can the app auto update?
- Explore owner mode instead of admin mode
- Integrate with Telegram?
- Explore websocket instead of polling
- Move to https
- Use device attestation for enrollment
- Enable threshold per group (eg group tabs 1+2, then threshold is set per tab)
- Add config, lock state resets at midnight or not

Done
- Add list of ignore, eg ignore godmin or launcher for threshold calculation
- Add config: server failure makes app fail open or closed
- When the provision config is changed, push it to the device
- Update push url
